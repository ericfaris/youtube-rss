# yt-dlp is exact-pinned for reproducible, reviewable builds. It ships frequent
# YouTube extractor fixes, so Renovate (see renovate.json) opens a PR on each new
# release to keep this current — bumps are CI-tested before they ship.
# [default] bundles yt-dlp-ejs (the YouTube n-challenge solver scripts).
yt-dlp[default]==2026.6.9
fastapi==0.138.0
uvicorn[standard]==0.49.0
apscheduler==3.11.2
feedgen==1.0.0
python-multipart==0.0.32
